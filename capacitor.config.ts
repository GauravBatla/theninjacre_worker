import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.theninjacare.worker',
  appName: 'The Ninja Care - Worker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
